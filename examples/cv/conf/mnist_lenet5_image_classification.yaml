defaults:
  # Load default model settings.
  - cv.dataset@model.training.dataset: mnist
  - dataloader@model.training.dataloader: dataloader
  - optim@model.optim: novograd
  # Load default trainer settings.
  - trainer: trainer
  # Overwrite the default settings.
  - mnist_lenet5_image_classification_overwrites