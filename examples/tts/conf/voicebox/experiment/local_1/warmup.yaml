# @package _global_

# to execute this experiment run:
# python train.py experiment=local-1/warmup

defaults:
  - /experiment@_global_:
    - local_1
    - tricks/adam_warmup

# task name, determines output directory path
task_name: "local_1-warmup"