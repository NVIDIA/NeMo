from setuptools import setup, find_packages

setup(
    name='triton',
    version='2.0.0',
    packages=find_packages(),
    description='A placeholder package that does nothing',
    install_requires=[],
)
