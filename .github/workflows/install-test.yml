name: CI-Install-Check

on:
  pull_request:
    paths:
      - "**"

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  test-installs:
    name: test-install-${{ matrix.os }}-py${{ matrix.python }}-${{ matrix.installer }}
    runs-on: ${{ matrix.os }}
    container: ${{ matrix.image }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        image: ["python:3.10", "python:3.11", "python:3.12", "nvcr.io/nvidia/pytorch:25.01-py3"]
        installer: ["pip", "reinstall.sh"]
    steps:
      - name: Checkout repo
        uses: actions/checkout@v2
      - name: Install NeMo
        env:
          INSTALLER: ${{ matrix.installer }}
          NEMO_TAG: ${{ github.sha }}
          NEMO_REPO: ${{ github.server_url }}/${{ github.repository }}
        shell: bash
        run: |
          if [[ "$INSTALLER" == "pip" ]]; then
            pip install .
          else
            export NEMO_TAG
            export NEMO_REPO
            export INSTALL_DIR=$(pwd)

            bash reinstall.sh --library nemo --mode install
          fi
